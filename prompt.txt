Tu travailles sur le projet AUDITSCANIA.

CONTEXTE — VULNÉRABILITÉ CONFIRMÉE MANUELLEMENT :
sqlmap -u http://juiceshop:3000/rest/user/login \
  --data='{"email":"test@test.com","password":"wrongpass123"}' \
  --method=POST -p email --batch --technique=B \
  --risk=3 --level=2 --ignore-code=401

→ CONFIRMÉ vulnérable :
  Parameter: JSON email ((custom) POST)
  Type: boolean-based blind
  Title: OR boolean-based blind - WHERE or HAVING clause (NOT)
  Payload: {"email":"test@test.com' OR NOT 2792=2792-- XRSi",...}
  back-end DBMS: SQLite
  (249 requêtes, ~9-12s)

C'est le bypass d'authentification SQLi classique de Juice Shop
(CWE-89, sévérité attendue CRITICAL ou HIGH — bypass d'auth).

Lis ces fichiers avant de commencer :
- backend/app/workers/scan_tasks.py (fonction _call_sqlmap_enriched)
- sqlmap/main.py (ScanRequest, EndpointInput, _BASE_FLAGS,
  _run_sqlmap_single, _build_target_list, et la fonction qui
  parse les résultats sqlmap pour remplir "vulnerable"/"findings"/"dbms")

Résume. Attendre confirmation.

---

## FIX G1 — sqlmap/main.py : --ignore-code + --risk pour les requêtes POST

Dans _run_sqlmap_single, branche `if method == "POST":`
(actuellement : cmd += ["--data", data]; cmd += ["--method=POST"]),
AJOUTER après ces lignes :
  cmd += ["--risk=3", "--ignore-code=401,403,500"]

Justification (commentaire à ajouter) :
  - --risk=3 : nécessaire pour les payloads OR-based (auth bypass)
  - --ignore-code=401,403,500 : les endpoints d'auth retournent
    401/403 pour des credentials invalides — sans ce flag, sqlmap
    abandonne au test de connectivité avant même de tester
    l'injection. 500 ajouté car observé pendant l'exploitation
    SQLite (87 occurrences) sans bloquer la détection.

Ne PAS modifier _BASE_FLAGS globalement (garder --risk=1 par
défaut pour les requêtes GET, qui sont la majorité et n'ont pas
besoin de cette agressivité).

Montrer avant/après. Attendre confirmation.

---

## FIX G2 — scan_tasks.py : ajouter la probe auth-bypass dans _call_sqlmap_enriched

Après la construction de `endpoints` depuis ZAP (et avant le
return/l'appel au microservice), ajouter une probe explicite
pour l'endpoint de login (Juice Shop : /rest/user/login) :

  _base_url = target if target.startswith(("http://","https://")) else f"http://{target}"
  _base_url = _base_url.rstrip("/")
  _login_probe_data = '{"email":"test@test.com","password":"wrongpass123"}'
  endpoints.append({
      "url":    f"{_base_url}/rest/user/login",
      "method": "POST",
      "params": ["email"],
      "data":   _login_probe_data,
  })

Ajouter un log : "[P3] SQLMap: probe auth-bypass ajoutée sur
/rest/user/login (email, JSON)".

Montrer avant/après. Attendre confirmation.

---

## FIX G3 — Vérifier le parsing des résultats sqlmap (severity/CWE)

Dans sqlmap/main.py, trouver la fonction qui transforme la sortie
sqlmap (CSV/log dans --output-dir) en {"vulnerable": bool,
"findings": [...], "dbms": str, "total": int}.

Vérifier qu'un résultat "boolean-based blind" avec DBMS détecté
produit bien un finding avec :
  - severity: "critical" ou "high" (auth bypass = critique)
  - cwe_ids: ["cwe-89"]
  - title incluant "SQL Injection" + "Authentication Bypass" si
    l'endpoint est /login ou /rest/user/login

Si la sévérité par défaut est trop basse (ex: "medium"), ajouter
une règle : si l'URL contient "login" ET technique="boolean-based
blind" trouvée → severity="critical" (CWE-89 + CWE-287 Improper
Authentication).

Montrer avant/après. Attendre confirmation.

---

Contraintes :
- Lire avant de modifier
- Montrer avant/après pour chaque fix
- Attendre confirmation après chaque fix
- Ne pas casser les targets GET existants (--risk=1 conservé pour eux)

