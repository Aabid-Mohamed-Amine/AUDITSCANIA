Tu travailles sur AUDITSCANIA.

CONTEXTE — TROU ARCHITECTURAL CONFIRMÉ :
sqlmap_data (microservice :9007) détecte correctement un SQLi
CRITICAL (CWE-89+CWE-287, auth bypass sur /rest/user/login),
mais correlator.py n'a JAMAIS reçu ni traité sqlmap_data — sa
fonction correlate() n'a même pas ce paramètre dans sa signature.
Résultat : correlated_data.by_severity ne contient jamais
"critical" même quand un vrai SQLi critique est détecté, et le
guard de scoring (score=max(score,75) si critical>=1) ne se
déclenche jamais.

Lis ces fichiers avant de commencer :
- backend/app/correlation_engine/correlator.py (def correlate(),
  Phase A Nuclei comme modèle, structure complète d'un finding)
- backend/app/workers/scan_tasks.py (l'appel à correlate(),
  où sqlmap_result est déjà disponible avant cet appel)

Résume. Attendre confirmation.

---

## FIX H — Intégrer sqlmap_data dans le Correlation Engine

### H1 — correlator.py : ajouter le paramètre et la Phase SQLi

1. Ajouter `sqlmap_data: Optional[Dict[str, Any]] = None` à la
   signature de correlate() (après wapiti_data, même pattern que
   les autres Optional avec default None).

2. Juste après `wapiti_data = wapiti_data or {}`, ajouter :
   `sqlmap_data = sqlmap_data or {}`

3. Ajouter une nouvelle phase "Phase H : Findings SQLMap" (après
   Phase A Nuclei, même style), qui itère sur
   sqlmap_data.get("findings", []) et ajoute à `findings` une
   entrée par finding SQLMap avec cette structure (suivre EXACTEMENT
   le pattern de Phase A) :

   findings.append({
       "id":                   f"sqlmap-{idx}",
       "type":                 "vulnerability",
       "title":                sf.get("title", "SQL Injection"),
       "severity":             sf.get("severity", "high"),
       "sources":              ["sqlmap"],
       "affected_service":     "",
       "affected_port":        _extract_port_from_url(sf.get("target_url", "")),
       "cve_ids":               [],
       "cwe_ids":               sf.get("cwe_ids", []),
       "cvss_score":            None,
       "epss_score":            None,
       "tags":                  ["sqli", "injection"],
       "exploitability_score":  95.0,  # SQLi confirmé = hautement exploitable
       "confidence_score":      0.95,  # confirmé activement par sqlmap, pas une heuristique
       "attack_path":           f"SQL Injection on {sf.get('parameter','')} → {sf.get('description','')}",
   })

   Ajouter aussi un attack_path lisible à attack_paths (même
   pattern que Phase A) : quelque chose comme
   f"SQLi confirmed: {sf.get('target_url','')} → {sf.get('description','')}"

Montrer avant/après. Attendre confirmation.

### H2 — scan_tasks.py : passer sqlmap_result à correlate()

Dans l'appel correlate(...), ajouter le paramètre :
   sqlmap_data = sqlmap_result,

ATTENTION : sqlmap_result n'existe que dans la branche
`if has_injectable_params and "sqlmap" in tools_to_run:` — si
cette branche n'a pas été exécutée (ne devrait plus arriver
depuis FIX G mais sécuriser quand même), utiliser
`sqlmap_result if 'sqlmap_result' in dir() else {}` ou s'assurer
qu'une variable sqlmap_result = {} est initialisée avant la
branche conditionnelle, pour éviter un NameError.

Montrer avant/après. Attendre confirmation.

---

Contraintes :
- Lire avant de modifier
- Montrer avant/après pour chaque fix
- Attendre confirmation après chaque fix
- Ne pas casser les autres phases de corrélation

